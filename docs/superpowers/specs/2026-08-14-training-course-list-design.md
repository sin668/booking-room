---
comet_change: training-course-list
role: technical-design
canonical_spec: openspec
---

# 培训课程列表页 — 深度技术设计

## 1. 上下文

本 Design Doc 基于 OpenSpec change `training-course-list` 的 proposal.md、design.md、specs 和 tasks.md，对高层方案进行深度技术细化。OpenSpec 产物是上游事实源，本文档聚焦实现方案、技术风险、测试策略和边界条件。

**运行环境**：
- Python 3.12.11 (conda): `conda activate booking-room`
- Node v22.22.0 (nvm): `nvm use v22.22.0`

## 2. 后端数据模型设计

### 2.1 Teacher 模型 (`br-server/app/models/teacher.py`)

```python
from datetime import datetime
from sqlalchemy import DateTime, Integer, Numeric, String, func
from sqlalchemy.orm import Mapped, mapped_column
from app.core.database import Base

class Teacher(Base):
    __tablename__ = "teachers"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(50), nullable=False)
    avatar: Mapped[str | None] = mapped_column(String(512), nullable=True)
    title: Mapped[str | None] = mapped_column(String(50), nullable=True)
    rating: Mapped[float] = mapped_column(Numeric(3, 1), default=0.0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=func.now(), onupdate=func.now(), nullable=False)
```

### 2.2 Course 模型 (`br-server/app/models/course.py`)

```python
from datetime import datetime
from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, Numeric, String, func
from sqlalchemy.orm import Mapped, mapped_column
from app.core.database import Base

class Course(Base):
    __tablename__ = "courses"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    room_id: Mapped[int] = mapped_column(Integer, ForeignKey("study_rooms.id"), nullable=False)
    teacher_id: Mapped[int | None] = mapped_column(Integer, ForeignKey("teachers.id"), nullable=True)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    cover_image: Mapped[str | None] = mapped_column(String(512), nullable=True)
    category: Mapped[str] = mapped_column(String(30), nullable=False)
    price: Mapped[float] = mapped_column(Numeric(10, 2), nullable=False)
    rating: Mapped[float] = mapped_column(Numeric(3, 1), default=0.0, nullable=False)
    enrollment_count: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    schedule: Mapped[str | None] = mapped_column(String(200), nullable=True)
    tags: Mapped[str | None] = mapped_column(String(200), nullable=True)
    status: Mapped[str] = mapped_column(String(20), default="active", nullable=False)
    is_hot: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    sort_order: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=func.now(), onupdate=func.now(), nullable=False)
```

### 2.3 StudyRoom 模型扩展 (`br-server/app/models/study_room.py`)

在现有 StudyRoom 模型中增加一个字段：

```python
room_type: Mapped[str] = mapped_column(String(20), default="study", nullable=False)
```

### 2.4 模型注册 (`br-server/app/models/__init__.py`)

在导入列表和 `__all__` 中添加 `Teacher` 和 `Course`。

### 2.5 关系映射策略

**不使用 ORM relationship**（`relationship()`, `back_populators` 等）。所有关联查询在 service 层通过显式 `select().join()` 完成。

**理由**：参考 BUG-16（MissingGreenlet），SQLAlchemy async session 下 ORM relationship 的懒加载会触发异常。显式 JOIN 更安全、更可控、性能可预测。

## 3. 数据库迁移策略

### 3.1 迁移文件 (`alembic/versions/xxxx_add_room_type_teachers_courses.py`)

```python
"""add room_type, teachers, courses

Revision ID: <auto-generated>
Revises: <previous_revision>
"""
from alembic import op
import sqlalchemy as sa

def upgrade() -> None:
    # 1. study_rooms 增加 room_type 列
    op.add_column("study_rooms",
        sa.Column("room_type", sa.String(20), server_default="study", nullable=False))

    # 2. 创建 teachers 表
    op.create_table("teachers",
        sa.Column("id", sa.Integer, primary_key=True, autoincrement=True),
        sa.Column("name", sa.String(50), nullable=False),
        sa.Column("avatar", sa.String(512), nullable=True),
        sa.Column("title", sa.String(50), nullable=True),
        sa.Column("rating", sa.Numeric(3, 1), server_default="0.0", nullable=False),
        sa.Column("created_at", sa.DateTime, server_default=sa.func.now(), nullable=False),
        sa.Column("updated_at", sa.DateTime, server_default=sa.func.now(), nullable=False),
    )

    # 3. 创建 courses 表
    op.create_table("courses",
        sa.Column("id", sa.Integer, primary_key=True, autoincrement=True),
        sa.Column("room_id", sa.Integer, sa.ForeignKey("study_rooms.id"), nullable=False),
        sa.Column("teacher_id", sa.Integer, sa.ForeignKey("teachers.id"), nullable=True),
        sa.Column("name", sa.String(100), nullable=False),
        sa.Column("cover_image", sa.String(512), nullable=True),
        sa.Column("category", sa.String(30), nullable=False),
        sa.Column("price", sa.Numeric(10, 2), nullable=False),
        sa.Column("rating", sa.Numeric(3, 1), server_default="0.0", nullable=False),
        sa.Column("enrollment_count", sa.Integer, server_default="0", nullable=False),
        sa.Column("schedule", sa.String(200), nullable=True),
        sa.Column("tags", sa.String(200), nullable=True),
        sa.Column("status", sa.String(20), server_default="active", nullable=False),
        sa.Column("is_hot", sa.Boolean, server_default="false", nullable=False),
        sa.Column("sort_order", sa.Integer, server_default="0", nullable=False),
        sa.Column("created_at", sa.DateTime, server_default=sa.func.now(), nullable=False),
        sa.Column("updated_at", sa.DateTime, server_default=sa.func.now(), nullable=False),
    )

    # 4. 创建索引
    op.create_index("ix_courses_room_id", "courses", ["room_id"])
    op.create_index("ix_courses_teacher_id", "courses", ["teacher_id"])
    op.create_index("ix_courses_category", "courses", ["category"])

def downgrade() -> None:
    op.drop_index("ix_courses_category", table_name="courses")
    op.drop_index("ix_courses_teacher_id", table_name="courses")
    op.drop_index("ix_courses_room_id", table_name="courses")
    op.drop_table("courses")
    op.drop_table("teachers")
    op.drop_column("study_rooms", "room_type")
```

### 3.2 迁移安全措施

- `room_type` 使用 `server_default='study'` 确保现有数据自动获得 `study` 类型
- `rating`, `enrollment_count`, `status`, `is_hot`, `sort_order` 均设 `server_default` 确保非空约束安全
- `created_at`/`updated_at` 使用 `sa.func.now()` 作为 server_default，不手动传 Python datetime（参考 BUG-15）

## 4. API Schema 设计

### 4.1 Teacher Schema (`br-server/app/schemas/teacher.py`)

```python
from pydantic import BaseModel, ConfigDict
from decimal import Decimal

class TeacherResponse(BaseModel):
    id: int
    name: str
    avatar: str | None = None
    title: str | None = None
    rating: Decimal

    model_config = ConfigDict(from_attributes=True)
```

### 4.2 Course Schema (`br-server/app/schemas/course.py`)

```python
from pydantic import BaseModel, ConfigDict, field_validator
from decimal import Decimal

class TeacherBrief(BaseModel):
    """教师简要信息，嵌套在课程响应中"""
    id: int
    name: str
    avatar: str | None = None
    title: str | None = None
    rating: Decimal

    model_config = ConfigDict(from_attributes=True)

class HotCourseItem(BaseModel):
    """热门课程简要信息，嵌套在培训室响应中"""
    id: int
    name: str
    cover_image: str | None = None
    teacher: TeacherBrief | None = None
    price: Decimal
    enrollment_count: int

class TrainingRoomResponse(BaseModel):
    """培训室列表响应中的单个培训室"""
    id: int
    name: str
    description: str | None = None
    cover_image: str | None = None
    address: str
    city_id: int | None = None
    city_name: str | None = None
    business_hours: str | None = None
    status: str
    room_type: str
    min_price: Decimal
    hot_courses: list[HotCourseItem] = []

    model_config = ConfigDict(from_attributes=True)

class TrainingRoomListResponse(BaseModel):
    items: list[TrainingRoomResponse]
    total: int
    page: int
    page_size: int

class CourseResponse(BaseModel):
    """课程列表响应中的单个课程"""
    id: int
    name: str
    cover_image: str | None = None
    teacher: TeacherBrief | None = None
    category: str
    price: Decimal
    rating: Decimal
    enrollment_count: int
    schedule: str | None = None
    tags: list[str] = []
    status: str
    room_id: int
    room_name: str

    model_config = ConfigDict(from_attributes=True)

    @field_validator("tags", mode="before")
    @classmethod
    def parse_tags(cls, v):
        """将逗号分隔字符串解析为列表，None 或空字符串返回空列表"""
        if v is None or v == "":
            return []
        return [tag.strip() for tag in v.split(",") if tag.strip()]

class CourseListResponse(BaseModel):
    items: list[CourseResponse]
    total: int
    page: int
    page_size: int
```

### 4.3 StudyRoom Schema 扩展 (`br-server/app/schemas/study_room.py`)

在 `StudyRoomResponse` 中增加 `room_type: str` 字段。
在 `RoomCreate` 和 `RoomUpdate` 中增加 `room_type: str | None` 可选字段。

## 5. Service 层实现

### 5.1 Training Service (`br-server/app/services/training_service.py`)

```python
from itertools import groupby
from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.city import City
from app.models.course import Course
from app.models.study_room import StudyRoom
from app.models.teacher import Teacher
from app.schemas.course import (
    CourseListResponse, CourseResponse, HotCourseItem,
    TrainingRoomListResponse, TrainingRoomResponse,
)
from app.schemas.teacher import TeacherResponse

MAX_PAGE_SIZE = 50
DEFAULT_PAGE_SIZE = 10

async def list_training_rooms(
    db: AsyncSession,
    page: int = 1,
    page_size: int = DEFAULT_PAGE_SIZE,
    city_id: int | None = None,
) -> TrainingRoomListResponse:
    page_size = min(page_size, MAX_PAGE_SIZE)
    offset = (page - 1) * page_size

    # Step 1: 查询培训室分页 (room_type IN [training, comprehensive], status=open)
    filters = [
        StudyRoom.status == "open",
        StudyRoom.room_type.in_(["training", "comprehensive"]),
    ]
    if city_id is not None:
        filters.append(StudyRoom.city_id == city_id)

    count_result = await db.execute(
        select(func.count()).select_from(StudyRoom).where(*filters))
    total = count_result.scalar_one()

    result = await db.execute(
        select(StudyRoom, City.name.label("city_name"))
        .outerjoin(City, StudyRoom.city_id == City.id)
        .where(*filters)
        .order_by(StudyRoom.id.asc())
        .offset(offset).limit(page_size))
    rooms = result.all()

    if not rooms:
        return TrainingRoomListResponse(items=[], total=total, page=page, page_size=page_size)

    room_ids = [room.id for room, _ in rooms]

    # Step 2: 批量查询热门课程 (JOIN teachers)
    hot_result = await db.execute(
        select(Course, Teacher)
        .outerjoin(Teacher, Course.teacher_id == Teacher.id)
        .where(
            Course.room_id.in_(room_ids),
            Course.is_hot == True,
            Course.status == "active",
        )
        .order_by(Course.room_id.asc(), Course.sort_order.asc(), Course.id.asc()))
    hot_rows = hot_result.all()

    # Step 3: Python groupby 分组，每组取前 3 条
    hot_by_room = {}
    for course, teacher in hot_rows:
        if course.room_id not in hot_by_room:
            hot_by_room[course.room_id] = []
        if len(hot_by_room[course.room_id]) < 3:
            hot_by_room[course.room_id].append(
                HotCourseItem(
                    id=course.id, name=course.name, cover_image=course.cover_image,
                    teacher=TeacherResponse.model_validate(teacher) if teacher else None,
                    price=course.price, enrollment_count=course.enrollment_count,
                ))

    # Step 4: 组装响应
    items = []
    for room, city_name in rooms:
        item = TrainingRoomResponse.model_validate(room)
        item.city_name = city_name
        item.hot_courses = hot_by_room.get(room.id, [])
        items.append(item)

    return TrainingRoomListResponse(items=items, total=total, page=page, page_size=page_size)


async def list_courses(
    db: AsyncSession,
    page: int = 1,
    page_size: int = DEFAULT_PAGE_SIZE,
    category: str | None = None,
) -> CourseListResponse:
    page_size = min(page_size, MAX_PAGE_SIZE)
    offset = (page - 1) * page_size

    filters = [Course.status == "active"]
    if category is not None:
        filters.append(Course.category == category)

    count_result = await db.execute(
        select(func.count()).select_from(Course).where(*filters))
    total = count_result.scalar_one()

    result = await db.execute(
        select(Course, StudyRoom.name.label("room_name"), Teacher)
        .join(StudyRoom, Course.room_id == StudyRoom.id)
        .outerjoin(Teacher, Course.teacher_id == Teacher.id)
        .where(*filters)
        .order_by(Course.sort_order.asc(), Course.id.asc())
        .offset(offset).limit(page_size))
    rows = result.all()

    items = []
    for course, room_name, teacher in rows:
        item = CourseResponse.model_validate(course)
        item.room_name = room_name
        item.teacher = TeacherResponse.model_validate(teacher) if teacher else None
        items.append(item)

    return CourseListResponse(items=items, total=total, page=page, page_size=page_size)
```

### 5.2 StudyRoom Service 扩展

在 `list_study_rooms` 和 `admin_list_rooms` 中增加 `room_type: str | None = None` 参数，当提供时添加 `StudyRoom.room_type == room_type` 过滤条件。

## 6. API Routes

### 6.1 Training Routes (`br-server/app/api/routes/training.py`)

```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.schemas.course import CourseListResponse, TrainingRoomListResponse
from app.services import training_service

router = APIRouter(prefix="/api/v1/training", tags=["training"])

@router.get("/rooms", response_model=TrainingRoomListResponse)
async def list_training_rooms(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1),
    city_id: int | None = Query(None, ge=1),
    db: AsyncSession = Depends(get_db),
) -> TrainingRoomListResponse:
    return await training_service.list_training_rooms(db, page=page, page_size=page_size, city_id=city_id)

@router.get("/courses", response_model=CourseListResponse)
async def list_training_courses(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1),
    category: str | None = Query(None, pattern="^(primaryschool|middleschool|postgraduate|civil_service|language|skills|professional)$"),
    db: AsyncSession = Depends(get_db),
) -> CourseListResponse:
    return await training_service.list_courses(db, page=page, page_size=page_size, category=category)
```

**注意**：路由路径使用 `/rooms` 和 `/courses`，不使用尾部斜杠（参考 BUG-22）。路由前缀为 `/api/v1/training`。

### 6.2 StudyRoom Routes 扩展

在现有 `list_study_rooms` 路由中增加 `room_type` 查询参数：

```python
room_type: str | None = Query(None, pattern="^(study|training|comprehensive)$"),
```

### 6.3 路由注册

在 `br-server/app/main.py` 中注册 `training_router`。

## 7. 前端页面架构

### 7.1 页面文件 (`br-app/src/pages/training/index.vue`)

**结构**：单文件页面，内联模板，不拆分为独立组件（遵循 br-app 现有惯例如 `study-record/index.vue`）。

**Script setup 导入规则**（参考 BUG-14）：
```javascript
import { ref, computed, onMounted } from 'vue'  // Vue 3 生命周期钩子从 vue 导入
import { onReachBottom } from '@dcloudio/uni-app'  // uni-app 页面钩子从 @dcloudio/uni-app 导入
```

**响应式状态**：
```javascript
const activeTab = ref('all')           // 当前选中 TAB: all/primaryschool/middleschool/civil_service/skills
const trainingRooms = ref([])          // 培训室列表
const courses = ref([])               // 课程列表
const loading = ref(false)            // 加载状态
const expandedRooms = ref(new Set())  // 展开热门课程的培训室 ID 集合
```

**TAB 配置**：
```javascript
const tabs = [
  { key: 'all', label: '全部' },
  { key: 'primaryschool', label: '小学辅导' },
  { key: 'middleschool', label: '中学辅导' },
  { key: 'civil_service', label: '公考备考' },
  { key: 'skills', label: '技能提升' },
]
```

**数据加载逻辑**：
- `activeTab === 'all'` → 调用 `getTrainingRooms()`
- `activeTab !== 'all'` → 调用 `getTrainingCourses({ category: activeTab })`
- `watch(activeTab)` 触发数据切换

### 7.2 API 模块 (`br-app/src/api/training.js`)

```javascript
import { get } from '@/utils/request'

export function getTrainingRooms(params) {
  return get('/api/v1/training/rooms', params)
}

export function getTrainingCourses(params) {
  return get('/api/v1/training/courses', params)
}
```

### 7.3 TabBar 修改 (`br-app/src/pages.json`)

在 `tabBar.list` 数组中第 3 位插入培训 tab：

```json
{
  "pagePath": "pages/training/index",
  "text": "培训",
  "iconPath": "static/tab/training.png",
  "selectedIconPath": "static/tab/training-active.png"
}
```

同时在 `pages` 数组中注册页面路由：

```json
{
  "path": "pages/training/index",
  "style": {
    "navigationBarTitleText": "培训课程",
    "navigationStyle": "custom"
  }
}
```

**图标文件**：需准备 `static/tab/training.png` 和 `static/tab/training-active.png`（graduation-cap 风格，建议 81x81px PNG）。

### 7.4 UI 设计参考

参考 `prototype/training.html` 高保真原型图：
- 配色：primary #4F6EF7，背景 #F5F6FA，卡片白色 rounded-2xl shadow-sm
- 顶部：自定义 NAV BAR "培训课程" + 搜索栏
- 分类 TAB 栏：横向 scroll-view，选中高亮下划线
- 培训室卡片：封面图 + 信息区 + 热门推荐课程展开按钮
- 课程卡片：封面图 + 信息区 + 预约按钮
- 动画：fadeInUp 入场，max-height 过渡展开/收起

## 8. 种子数据

在 `br-server/app/services/seed_data.py` 中增加：
- 3 间培训室（room_type=training）
- 1 间综合室（room_type=comprehensive）
- 约 5 位教师数据
- 约 10 条课程数据，覆盖 primaryschool/middleschool/civil_service/skills 分类
- 部分课程设置 `is_hot=true`
- 部分课程关联教师（teacher_id 非 null），部分不关联（teacher_id=null）

**时间处理**（参考 BUG-15）：不使用 `datetime.now(UTC)`，依赖数据库 `func.now()` 默认值。

## 9. 测试策略

### 9.1 后端测试 (`br-server/tests/test_training_api.py`)

| 测试用例 | 验证内容 |
|---------|---------|
| test_list_training_rooms_default | 默认分页、返回 hot_courses 字段 |
| test_list_training_rooms_filter_city | city_id 过滤 |
| test_comprehensive_room_appears | 综合室出现在培训室列表 |
| test_study_room_excluded | 自习室不出现在培训室列表 |
| test_hot_courses_limit_3 | 热门课程最多 3 条 |
| test_hot_courses_include_teacher | 热门课程含教师信息 |
| test_training_room_no_hot_courses | 无热门课程时 hot_courses=[] |
| test_list_courses_default | 课程列表默认分页 |
| test_list_courses_filter_category | category 过滤 |
| test_course_teacher_nested | teacher 嵌套对象 |
| test_course_without_teacher | teacher_id=null → teacher=null |
| test_course_tags_parsing | tags 逗号分隔 → 数组 |
| test_course_empty_tags | tags=null → [] |

### 9.2 现有测试更新 (`br-server/tests/test_api_homepage.py`)

| 测试用例 | 验证内容 |
|---------|---------|
| test_room_type_filter | room_type=study 过滤 |
| test_room_type_in_response | 响应包含 room_type 字段 |

### 9.3 验证命令

```bash
# 后端测试
conda activate booking-room && cd br-server && pytest tests/ -q

# 前端构建
nvm use v22.22.0 && cd br-app && npm run build
```

## 10. BUG 防范清单

| BUG | 防范措施 | 涉及文件 |
|-----|---------|---------|
| BUG-14 | `onMounted` 从 `vue` 导入，`onLoad`/`onShow` 从 `@dcloudio/uni-app` 导入 | training/index.vue |
| BUG-15 | 迁移和种子数据中 timestamps 使用 `func.now()`，不传 `datetime.now(UTC)` | migration, seed_data.py |
| BUG-20 | 前端模板中不使用 `&lt;`/`&gt;`，如需箭头用 Unicode `‹`/`›` | training/index.vue |
| BUG-22 | 新路由 `@router.get("/rooms")` 和 `@router.get("/courses")` 无尾部斜杠 | training.py |
| BUG-16 | 不使用 ORM relationship，service 层显式 JOIN | models, training_service.py |
| BUG-13 | 前端 API 中 page_size 不超过 50 | training.js |

## 11. 无 Spec Patch

OpenSpec delta spec 中的验收场景已充分覆盖功能需求，无需回写或补充。
