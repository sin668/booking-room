## 1. 后端模型、迁移与领域边界

- [x] 1.1 阅读 `br-server/app/models/activity.py`、`br-server/app/models/coupon.py`、`br-server/app/models/booking.py`，确认活动、卡券模板、用户卡券和预约用券的现有字段与关系
- [x] 1.2 确认活动详情富文本字段归属 `Activity.content_html`，卡券优惠规则继续归属 `Coupon`，活动发放规则归属新增活动卡券配置
- [x] 1.3 在 `Activity` 模型中增加 `content_html` 字段，用于保存清洗后的活动详情富文本正文
- [x] 1.4 新增 `ActivityCoupon` 模型，关联 `activity_id` 与 `coupon_id`
- [x] 1.5 在 `ActivityCoupon` 模型中增加 `total_quantity`、`claimed_quantity`、`per_user_limit`、`claim_starts_at`、`claim_ends_at`、`is_active`、`sort_order`、`display_title`、`display_description`
- [x] 1.6 为 `ActivityCoupon` 增加必要索引和约束，至少覆盖活动查询、卡券查询、库存非负、每人限领大于 0
- [x] 1.7 为 `UserCoupon` 增加 `source_type`、`source_activity_id`、`source_activity_coupon_id` 字段，用于记录活动领券来源
- [x] 1.8 编写 alembic 迁移：为 `activities` 增加 `content_html`，创建 `activity_coupons` 表，扩展 `user_coupons` 来源字段
- [x] 1.9 编写 alembic downgrade，确保可回退新增字段和表结构
- [x] 1.10 使用 `conda activate booking-room` 后运行迁移升级与回退验证，确认迁移脚本可执行

## 2. 后端 Schema、富文本清洗与公共校验

- [x] 2.1 扩展活动管理端请求 schema，支持 `content_html` 和 `activity_coupons`
- [x] 2.2 扩展活动管理端响应 schema，返回 `content_html`、活动卡券配置、已领取数量和关联卡券信息
- [x] 2.3 扩展用户端活动详情响应 schema，返回活动基础信息、`content_html`、活动卡券列表、剩余库存和用户领取状态
- [x] 2.4 新增活动领券响应 schema，返回新创建的用户卡券信息和最新活动卡券领取状态
- [x] 2.5 实现富文本安全清洗工具，保存前过滤 `<script>`、事件属性、危险链接和不在白名单内的标签
- [x] 2.6 为富文本清洗工具补充单元测试，覆盖正常排版、图片、链接、脚本、事件属性和空正文
- [x] 2.7 抽取活动卡券领取条件校验，覆盖活动上架、活动卡券启用、卡券模板启用、领取时间、库存、每人限领和重复领取
- [x] 2.8 为活动卡券校验补充单元测试，覆盖成功、未开始、已结束、库存不足、超出限领和卡券禁用

## 3. 后端管理端活动能力

- [x] 3.1 扩展活动创建逻辑，保存活动基础信息、清洗后的 `content_html` 和活动卡券配置
- [x] 3.2 扩展活动编辑逻辑，支持更新 `content_html`、新增活动卡券、编辑活动卡券、删除未领取的活动卡券配置
- [x] 3.3 对已产生领取记录的活动卡券增加编辑保护，禁止将总库存调整为小于已领取数量
- [x] 3.4 扩展管理端活动详情接口，返回活动卡券配置列表和富文本正文
- [x] 3.5 扩展管理端活动列表接口，返回关联卡券数量和已领取数量汇总
- [x] 3.6 扩展活动发布或上架逻辑，校验启用活动卡券必须关联启用卡券模板且库存配置有效
- [x] 3.7 为管理端活动创建、编辑、详情、列表、发布校验补充集成测试

## 4. 后端用户端活动详情与领券

- [x] 4.1 新增或扩展 `GET /api/v1/activities/{activity_id}/`，只返回已上架活动详情
- [x] 4.2 用户端活动详情接口返回清洗后的 `content_html`，无正文时返回空字符串或空值并保持结构稳定
- [x] 4.3 用户端活动详情接口返回可展示活动卡券列表，仅包含活动卡券启用、卡券模板启用且未超过领取结束时间的配置
- [x] 4.4 用户端活动详情接口在用户已登录时返回每个活动卡券的领取状态和剩余可领取次数
- [x] 4.5 实现 `POST /api/v1/activities/{activity_id}/coupons/{activity_coupon_id}/claim`
- [x] 4.6 领券接口必须要求登录，未登录返回 HTTP 401 且不创建用户卡券
- [x] 4.7 领券接口在数据库事务内锁定活动卡券配置，校验库存并递增 `claimed_quantity`
- [x] 4.8 领券成功后创建 `user_coupons`，写入活动来源字段并复用现有卡券有效期和使用规则
- [x] 4.9 领券失败时返回稳定错误码和中文错误信息，覆盖库存不足、超出限领、活动下架、卡券禁用、不在领取时间
- [x] 4.10 为用户端活动详情和领券接口补充集成测试
- [x] 4.11 为活动来源卡券参与用户卡券列表和预约可用卡券查询补充回归测试

## 5. br-admin 活动管理页面

- [x] 5.1 阅读 br-admin 现有活动管理页面、活动编辑弹窗、活动 API 封装和路由结构，确认复用方式
- [x] 5.2 扩展 br-admin 活动 API 类型，加入 `content_html`、`activity_coupons`、卡券数量和领取汇总字段
- [x] 5.3 扩展活动创建/编辑请求封装，提交 `content_html` 和活动卡券配置
- [x] 5.4 在活动创建/编辑表单中新增富文本正文编辑区域，支持输入、保存、编辑回显和空正文
- [x] 5.5 选择并接入项目已有富文本编辑能力；若项目无现成编辑器，优先选用与 Vue3/Vite2 兼容且维护活跃的轻量编辑器
- [x] 5.6 在活动创建/编辑表单中新增关联卡券配置区域，支持新增一项卡券配置
- [x] 5.7 活动卡券配置区域支持卡券类型、优惠规则、卡券有效期、总库存、每人限领、领取时间、展示文案、启用状态和排序
- [x] 5.8 活动卡券配置区域支持删除未保存配置、标记删除已保存配置、启停和排序
- [x] 5.9 增加表单校验：卡券类型必填、库存非负、每人限领大于 0、领取结束时间不得早于开始时间
- [x] 5.10 活动列表展示关联卡券数量和已领取数量汇总
- [x] 5.11 发布包含启用活动卡券的活动时展示“将同步展示关联卡券”的中文确认提示
- [x] 5.12 后端返回富文本或卡券配置错误时，在表单或消息提示中展示中文错误原因
- [x] 5.13 使用 `nvm use v22.22.0` 后运行 br-admin lint、类型检查或构建验证
- [ ] 5.14 人工冒烟验证 br-admin 活动创建、编辑、富文本回显、卡券配置、发布提示和列表汇总

## 6. br-app 首页、活动详情与领券

- [x] 6.1 阅读 br-app 首页活动区域、页面路由、活动 API、登录态判断和卡券包页面，确认复用方式
- [x] 6.2 扩展 br-app 活动 API 封装，新增活动详情查询和活动领券请求
- [x] 6.3 在首页热门活动卡片增加点击跳转，携带活动 ID 到活动详情页
- [x] 6.4 注册活动详情页路由，页面路径保持与项目 pages 结构一致
- [x] 6.5 新增活动详情页基础布局，展示封面、标题、摘要描述和参与人数
- [x] 6.6 使用小程序安全富文本组件渲染 `content_html`，正文为空时不展示空白模块
- [x] 6.7 活动详情页展示活动卡券列表，包含展示标题、说明、卡券类型、优惠规则、有效期、剩余库存和每人限领
- [x] 6.8 根据后端状态展示可领取、已领取、已抢光、未开始、已结束按钮或标签
- [x] 6.9 实现领取交互，已登录用户点击后调用领券接口，成功后提示并刷新详情
- [x] 6.10 未登录用户点击领取时引导登录，不展示领取成功状态
- [x] 6.11 领券失败时展示后端中文错误信息，并重新请求活动详情刷新状态
- [x] 6.12 确认活动来源卡券在卡券包页面正常展示，必要时展示“活动领取”来源文案
- [x] 6.13 确认活动来源卡券在预约可用卡券页面按既有规则可用或不可用
- [x] 6.14 使用 `nvm use v22.22.0` 后运行 br-app lint、类型检查或构建验证
- [ ] 6.15 人工冒烟验证首页跳转、活动详情富文本、未登录领券、成功领券、库存不足、超出限领和状态刷新

## 7. API 文档、OpenSpec 与交付验证

- [x] 7.1 更新 `docs/api.md` 管理端活动创建接口，补充 `content_html` 和 `activity_coupons` 请求示例
- [x] 7.2 更新 `docs/api.md` 管理端活动详情和列表接口，补充活动卡券配置、卡券数量和领取汇总响应字段
- [x] 7.3 更新 `docs/api.md` 用户端活动详情接口，补充 `content_html`、`activity_coupons`、领取状态和响应示例
- [x] 7.4 更新 `docs/api.md` 活动领券接口，补充请求路径、鉴权要求、成功响应、错误码和错误信息
- [x] 7.5 检查 `openspec/changes/activity-coupon-campaign/proposal.md`、`design.md`、`specs/**/*.md` 与本任务清单是否一致
- [x] 7.6 使用 `openspec status --change "activity-coupon-campaign"` 确认 proposal、design、specs、tasks 均完成
- [x] 7.7 使用 `conda activate booking-room` 后运行后端相关测试，至少覆盖活动、卡券和预约用券回归
- [x] 7.8 使用 `nvm use v22.22.0` 后运行 br-admin 与 br-app 可用的前端验证命令
- [x] 7.9 汇总验证结果，记录未能执行的命令、原因和剩余风险

## 8. 代码审查与重构检查

- [x] 8.1 检查后端是否保持 routes、schemas、services、models 分层清晰，活动卡券规则不得散落在路由层
- [x] 8.2 检查富文本清洗逻辑是否集中在后端公共工具或服务中，前端不得承担安全清洗责任
- [x] 8.3 检查活动领券和既有卡券使用逻辑是否复用公共校验，避免重复金额、有效期、状态和适用范围判断
- [x] 8.4 检查事务和并发控制，确保库存扣减和用户卡券创建在同一事务内完成
- [x] 8.5 检查 br-admin 表单组件拆分，避免活动编辑弹窗承担富文本、卡券配置和提交编排之外的过多职责
- [x] 8.6 检查 br-app 活动详情页组件拆分，必要时拆出富文本展示、卡券列表和领取按钮状态组件
- [x] 8.7 检查所有新增用户可见文案为中文，错误提示可读且与后端错误语义一致
- [x] 8.8 检查回滚路径：关闭领券入口、停用活动卡券配置时不得删除用户已领取卡券

